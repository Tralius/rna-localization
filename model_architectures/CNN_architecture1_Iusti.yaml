# Initial Architecture with updated values on the basis of RNA "Zip Codes" length
param_dataLoader_train:
  batch_size: 32
  shuffle: True

param_dataLoader_valid:
  batch_size: 32

params_model:
  architecture: cpdcpdcpdfede
  conv:
    - filters: 32
      kernel_size: 17
      activation: relu
    - filters: 64
      kernel_size: 11
      activation: relu
      padding: same
    - filters: 92
      kernel_size: 7
      activation: relu
      padding: same
  dense:
    - units: 512
      activation: relu
    - units: 9
      activation: softmax
  pooling:
    - pool_size: 4
    - pool_size: 4
    - pool_size: 4
  dropouts:
    - rate: 0.3
    - rate: 0.3
    - rate: 0.3
    - rate: 0.3
  #skip:
  #  - index: [0, 1]  # adding index
  #  - 0

params_train:
  epochs: 10
  use_multiprocessing: False

