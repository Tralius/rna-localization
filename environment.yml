name: reggen
channels:
  - defaults
dependencies:
  - pip:
    - pandas
    - torch
    - numpy
    - keras
    - jupyter
    - tensorflow
    - matplotlib
    - scikit-learn
prefix: /home/usr/anaconda3/envs/reggen
