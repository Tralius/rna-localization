name: reggen
channels:
  - defaults
dependencies:
  - pip:
    - pandas
    - torch
    - numpy
    - keras
    - tensorflow
    - jupyter
    - matplotlib
    - scikit-learn
    - pydot
    - tensorflow_probability
    - seaborn
prefix: /home/usr/anaconda3/envs/reggen
